-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    xinerama_head = 0,
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=7',
    gap_x = 5,
    gap_y = 5,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_argb_value = 128,
    own_window_argb_visual = true,
    own_window_transparent = false,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = true,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'left',
    use_xft = true,
}

conky.text = [[
$hr
${color grey}Uptime:$color $uptime
$hr
${color grey}CPU Usage:$color $cpu%
${cpugraph 25,200}
${color grey}GPU Usage: ${nvidia gpuutil}% 
${nvidiagraph gpuutil 25,200 GPU_0}
${color grey}GPU Temp: ${nvidia gputemp}°C
${nvidiagraph gputemp 25,200 GPU_0}
${color grey}RAM Usage:
$color $mem/$memmax - $memperc% 
${membar}
${color grey}Swap Usage:$color $swap/$swapmax - $swapperc% 
${swapbar 4}
$hr
${color grey}File systems:
 ${color white}SSD $color${fs_used /}/${fs_size /} 
${fs_bar 6 /}
 ${color white}HDD $color${fs_used /mnt/sda1}/${fs_size /mnt/sda1}
${fs_bar 6 /mnt/sda1}
 ${color white}M.2 $color${fs_used /mnt/nvme0n1p1}/${fs_size /mnt/nvme0n1p1} 
${fs_bar 6 /mnt/nvme0n1p1}
$hr





















$hr
${color grey}Network:
${color grey}Speed:     Up-$color${upspeed enp0s31f6} ${color grey}Down-$color${downspeed enp0s31f6}
${upspeedgraph enp0s31f6 50,100}  ${downspeedgraph enp0s31f6 50,100}

${color grey}Total:     Up-$color${totalup enp0s31f6} ${color grey}Down-$color${totaldown enp0s31f6}
$hr
]]
